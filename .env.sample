# note MCP Server 設定

# デバッグモード（詳細ログを出力）
DEBUG=false

# note.com認証情報（必須）
# 初回起動時にPlaywrightでブラウザログインを実行し、
# セッション情報は ~/.note-mcp-session.json に自動保存されます
NOTE_EMAIL=your-email@example.com
NOTE_PASSWORD=your-password

# Playwright設定（オプション）
# Windowsではデフォルトでheadlessモードを使用
# ブラウザを表示してログインしたい場合は false に設定
# PLAYWRIGHT_HEADLESS=false

# HTTPサーバー設定（VPSホスティング用、オプション）
# MCP_HTTP_PORT=3000
# MCP_HTTP_HOST=0.0.0.0
