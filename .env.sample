# note MCP Server 環境変数
# 初回起動時にブラウザでログインすると自動で設定されます

# === note.com 認証 ===
# 自動ログインを使う場合は以下を設定（任意）
# NOTE_EMAIL=your-email@example.com
# NOTE_PASSWORD=your-password
# NOTE_USER_ID=your-urlname

# セッションCookieを直接指定する場合（任意）
# NOTE_SESSION_V5=xxx
# NOTE_XSRF_TOKEN=xxx

# === Notion Integration ===
# https://www.notion.so/my-integrations で作成したTokenを設定
# NOTION_TOKEN=secret_...

# === 通知設定（任意）===
# WEBHOOK_URL=https://hooks.slack.com/services/xxx
# WEBHOOK_FORMAT=slack
# TELEGRAM_BOT_TOKEN=bot123:xxx
# TELEGRAM_CHAT_ID=123456

# === Twitter/X API v2（任意）===
# Free tier: $0/月、1500投稿/月
# https://developer.x.com/en/portal/dashboard
# TWITTER_API_KEY=xxx
# TWITTER_API_SECRET=xxx
# TWITTER_ACCESS_TOKEN=xxx
# TWITTER_ACCESS_SECRET=xxx

# === Threads API（任意）===
# Meta Developer Portal でアプリ作成 → App Review通過後に使用可能
# OAuth2.0でlong-lived tokenを取得して設定
# THREADS_ACCESS_TOKEN=xxx
# THREADS_USER_ID=xxx

# === SNS Webhook（任意）===
# Twitter/Threads以外のSNSにWebhook経由で中継する場合
# SNS_WEBHOOK_URL=https://your-webhook-endpoint.com/post

# === HTTPモード設定（任意）===
# MCP_HTTP_PORT=3000
# MCP_HTTP_HOST=127.0.0.1

# === 自律エージェント設定 ===
# AGENT_ENABLED=true
# AGENT_MODEL=sonnet
# AGENT_MAX_BUDGET_USD=0.50
# AGENT_DAILY_BUDGET_USD=5.00
# CLAUDE_CLI_PATH=claude

# === 投稿権限・頻度制御 ===
# dry-run-only: 実投稿しない（デフォルト）、full-auto: 実投稿する
# AGENT_POST_MODE=dry-run-only
# 1日あたりの最大投稿数（デフォルト: 3）
# AGENT_MAX_TWEETS_PER_DAY=3

# === アウトバウンドエンゲージメント上限 ===
# 1日あたりの最大いいね数（デフォルト: 20）
# AGENT_MAX_LIKES_PER_DAY=20
# 1日あたりの最大フォロー数（デフォルト: 5）
# AGENT_MAX_FOLLOWS_PER_DAY=5

# === デバッグ ===
# DEBUG=true
